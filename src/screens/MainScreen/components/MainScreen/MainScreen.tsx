import * as React from "react";

interface IProps {}

type Props = IProps;

const MainScreen: React.FC<Props> = (props: Props) => {
    return (
        <div>Main screen</div>
    );
};

export default MainScreen;
